//! Session persistence: Session, Turn and Evidence + JSONL append.
//!
//! Folder (not a loose file) so `jsonl.rs`, `store.rs` can grow here
//! without touching siblings. All Modes consume it via `shared/`.
